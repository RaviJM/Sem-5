#include <bits/stdc++.h>
#include <map>
#include <set>
using namespace std;

int main(){
    string st = "A";
    if (st[0] == 'A'){
        if (st[1] == ' '){
            cout<<"yes";
        }
    }
    return 0;
}